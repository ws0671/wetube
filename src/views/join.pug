extend base

block content 
    if errorMessage 
        span.error-span=errorMessage
    form(method="POST")
        input(placeholder='ID',name='username' , type='text',required)
        input(placeholder='Password',name='password', type='password',required)
        input(placeholder='Password Confirm',name='password2', type='password',required)
        input(placeholder='Nickname',name='name' , type='text',required)
        input(placeholder='Email',name='email' , type='email',required)
        input(type='submit',value='Join')
        include partials/social-login 
    div.auth-switch
        span Already have an account? 
        a(href='/login') Log in now &rarr;